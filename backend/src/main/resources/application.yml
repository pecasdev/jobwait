spring:
    application:
        name: jobwait
    datasource:
        driver-class-name: org.postgresql.Driver
        url: jdbc:postgresql://localhost:5432/mydatabase
        username: postgres
        password: password
